import PopupWithForm from "./PopupWithForm.js";

class PopupWithAvatar extends PopupWithForm {
    constructor(popupSelector, submitForm) {
        super(popupSelector, submitForm);
    }
}

export default PopupWithAvatar;